"use client";

import { ToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";

export const Snackbar = () => {
  return <ToastContainer position="bottom-center" theme="dark" />;
};
